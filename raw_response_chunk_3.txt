[{"question": "What is the microtask queue order when a Promise resolves inside a setTimeout callback, and a resolved Promise is already in the queue?","options": ["The setTimeout callback runs first, then its Promise resolution microtask, then the existing microtask","The existing microtask runs first, then the setTimeout callback, then its resolution microtask","The setTimeout callback and its resolution microtask run before any existing microtasks","All microtasks, including the one from setTimeout, run before the next macrotask, but order depends on queue insertion"],"correct_answer": "All microtasks, including the one from setTimeout, run before the next macrotask, but order depends on queue insertion"},{"question": "Which of the following code patterns can lead to a memory leak due to unresolved Promise handlers in a long-running application?","options": ["Using .finally() without catching rejections","Attaching .then() handlers to a Promise that never settles, but keeping references to those handlers","Using await in a try block without a catch","Returning a Promise from a synchronous function"],"correct_answer": "Attaching .then() handlers to a Promise that never settles, but keeping references to those handlers"},{"question": "What is the behavior of Promise.resolve() when called with a thenable object that has a .then method that throws synchronously?","options": ["It returns a rejected Promise with that thrown error","It returns a resolved Promise with the thenable as the value","It throws synchronously outside the Promise context","It returns a pending Promise that never settles"],"correct_answer": "It returns a rejected Promise with that thrown error"},{"question": "In an async function, if you have multiple await statements, and the first one throws, what happens to the second await?","options": ["It executes normally because they are independent","It is skipped entirely as the function jumps to the nearest catch block or exits","It executes but the result is ignored","It throws the same error again"],"correct_answer": "It is skipped entirely as the function jumps to the nearest catch block or exits"},{"question": "Which optimization technique minimizes the number of microtask ticks when resolving a chain of dependent asynchronous operations?","options": ["Using .then() instead of await for each step","Flattening nested Promises by returning values directly instead of wrapping in Promise.resolve","Using setImmediate to break the chain","Using async/await with multiple awaits in sequence"],"correct_answer": "Flattening nested Promises by returning values directly instead of wrapping in Promise.resolve"},{"question": "What is the result of the following code?\nlet p = Promise.reject('err');\nprocess.nextTick(() => p.catch(() => console.log('caught')));\n// Assume Node.js environment","options": ["It prints 'caught'","It throws an unhandled rejection error before the nextTick runs","It prints nothing due to the microtask order","It causes a deadlock"],"correct_answer": "It throws an unhandled rejection error before the nextTick runs"},{"question": "Which of the following is a correct way to implement a semaphore using Promises to limit concurrent async operations?","options": ["Using a counter and a queue of resolve functions, where each operation calls a release function on completion","Using Promise.all with a fixed array of promises","Using a for loop with await inside to sequentially limit concurrency","Using Promise.race to abort excess operations"],"correct_answer": "Using a counter and a queue of resolve functions, where each operation calls a release function on completion"},{"question": "Given a Promise that resolves after 100ms, what is the effect of calling .then() on it after it has already resolved?","options": ["The .then() callback is invoked synchronously","The .then() callback is scheduled as a microtask and executes after the current synchronous code finishes","The .then() callback is ignored because the Promise is already settled","The .then() callback executes after 100ms again"],"correct_answer": "The .then() callback is scheduled as a microtask and executes after the current synchronous code finishes"},{"question": "What is the output of this code in a browser?\n(async function test() {\n  console.log(1);\n  await Promise.resolve();\n  console.log(3);\n})();\nconsole.log(2);","options":